# AutoFlow Studio

Static GitHub Pages build of AutoFlow Studio.

Live deployment is configured through GitHub Actions using `.github/workflows/deploy-pages.yml`.

The web app is static and does not need a paid API. The local Playwright runner is intentionally not hosted on GitHub Pages because Pages only serves static files.
